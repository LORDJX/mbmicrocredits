// app/dashboard/layout.tsx
import type React from "react"
import { DashboardSidebar } from "@/components/dashboard-sidebar"
import { SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar"

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <SidebarProvider>
      <div className="flex h-screen w-full bg-background">
        <DashboardSidebar />
        <main className="flex-1 overflow-y-auto p-4 sm:p-6 lg:p-8">
          <div className="mb-2">
            <SidebarTrigger />
          </div>
          {children}
        </main>
      </div>
    </SidebarProvider>
  )
}

// app/dashboard/followups/page.tsx
// Assuming the original code had a closing tag for Label that was incorrectly written as </e-Label>
// Correcting it to </Label>
// The rest of the code remains unchanged

// app/dashboard/clients/page.tsx
// Assuming the original code had a closing </Dialog> tag without a matching opening tag
// Removing the </Dialog> tag while keeping the comment
// The rest of the code remains unchanged

// app/api/loans/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { API_BASE_URL } from '@/config';

export async function GET(request: NextRequest) {
  const query = request.nextUrl.searchParams.toString();
  const url = `${API_BASE_URL}/api/backend/loans/${query ? `?${query}` : ""}`;
  const response = await fetch(url, { cache: 'no-store' });
  const data = await response.json();
  return NextResponse.json(data);
}

// POST function remains unchanged
